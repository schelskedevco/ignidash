import { SimulationState } from './simulation-engine';

import { IncomesData } from './incomes';

export class TaxProcessor {
  constructor(private simulationState: SimulationState) {}

  process(): void {
    return;
  }

  processIncomeTax(incomesData: IncomesData): void {
    return;
  }
}
